# The privacy SDK is published to GitHub Packages, not npmjs, so the
# @starkware-libs scope has to be pointed at that registry explicitly.
# Reads there require authentication even though the package is public —
# put the token in ~/.npmrc, never here (this file is committed).
@starkware-libs:registry=https://npm.pkg.github.com
