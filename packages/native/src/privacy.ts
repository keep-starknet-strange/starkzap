/**
 * Privacy pool: `starkzap-native/privacy`.
 *
 * Forwards `starkzap/privacy`, so the native package mirrors core's entry
 * points. It is a separate entry because the privacy SDK is an optional peer.
 */
export * from "starkzap/privacy";
